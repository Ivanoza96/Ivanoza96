name: Rebuild showcase assets

on:
  push:
    paths:
      - 'showcase_payload/trigger.txt'

permissions:
  contents: write

jobs:
  rebuild:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Decode high-resolution showcase assets
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p assets/showcase
          cat showcase_payload/chunk-*.txt > /tmp/showcase-assets.zip.b64
          base64 --decode /tmp/showcase-assets.zip.b64 > /tmp/showcase-assets.zip
          unzip -o /tmp/showcase-assets.zip -d assets/showcase
          file assets/showcase/*.webp

      - name: Commit rebuilt assets and clean temporary payload
        shell: bash
        run: |
          set -euo pipefail
          rm -rf showcase_payload
          rm -f .github/workflows/rebuild-showcase.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Replace showcase previews with crisp original runtime captures'
          git push
